# Copy to .env to override local Docker defaults. Do not commit .env.
POSTGRES_DB=vehicle_reid
POSTGRES_USER=vehicle_reid
POSTGRES_PASSWORD=change-this-local-password

# PostgreSQL is the default deployment storage. Use sqlite only for reference/debug runs.
GALLERY_STORAGE=postgres
PORT=8000
