# Results

Notebook сохранит здесь HPO split, JSON каждого trial, сводку study, результаты top-4
после 20 эпох, результаты top-2 на трёх seed, выбранную конфигурацию, историю внешних
30 эпох, индекс checkpoint и итоговую ONNX-сводку.

`optuna.sqlite3` исключён из Git, но остаётся локально для возобновления study.
