# Журнал работ — 19 сентября 2026

## Выполнено

- Перенесены Dockerfile, docker-compose.yml, .dockerignore и настройка bind-адреса backend в Git-проект на ветке `devops`.
- Проверена конфигурация `docker compose`.
- Docker-образ успешно собран; внутри контейнера проверены аргументы запуска backend и загрузка ONNX-модели.

## Ограничения проверки

- В рабочей папке нет `dataset/`, поэтому запуск HTTP-сервиса с реальной gallery пока не выполнялся.
- PostgreSQL и pgvector ещё не подключены: текущим хранилищем остаётся SQLite.
- Подготовлен `docs/PROJECT_PLAN.md` для согласования следующего этапа: Docker-проверка, PostgreSQL + pgvector, тестирование и сдача.
- TASK 0 начат: Compose с добавленным датасетом успешно поднят, SQLite gallery содержит 750 объектов, `/api/health` отвечает, выполнен ranking-запрос для первого официального query.
- Исправлен Dockerfile: в образ добавлен корневой `evaluate.py`, требуемый `backend/scoring.py` на ветке `devops`.
- SQLite baseline export завершён и прошёл `--validate-only`: 1110 query, 750 gallery, `embeddings.npy` размера `(1860, 512)`, 1110 строк `submission.csv`, 1051 принятый query и 59 отказов.
- Зафиксирован baseline: threshold `0.5948754549026489`, validation mAP@10 `0.8146886982413298`, Rank-1 `0.8016194331983806`, candidate score `0.747121224071299`.
- TASK 1 завершён: добавлен `backend/gallery_repository.py` с `GalleryRepository` и SQLite reference implementation; `Gallery` использует repository abstraction.
- После рефакторинга: 39 тестов прошли; Docker Compose пересобран, API ready, SQLite gallery cache подхватился без повторного OSNet inference.
- Создан `tests/fixtures/sqlite_baseline.json` с embeddings, raw Top-50, reranked Top-10 и refusal для трёх контрольных query.

## TASK 2 — PostgreSQL + pgvector infrastructure

- Added the internal `postgres` Compose service using the pinned official image `pgvector/pgvector:0.8.6-pg16-bookworm`.
- Added a healthcheck and the persistent `postgres_data` volume; PostgreSQL has no host port exposure.
- Added `.env.example`; `.env` is ignored. `GALLERY_STORAGE` intentionally remains `sqlite` until PostgreSQL parity is proven.
- Validated `docker compose config`; PostgreSQL is healthy and `CREATE EXTENSION vector` reports version `0.8.6`.
- Recreated the API service through Compose and verified `/api/health` remains ready with a 750-item gallery.

## TASK 2 — completed

- Added `DatabaseSettings`, `PostgresGalleryRepository`, and `python -m backend.db_probe`.
- PostgreSQL settings are read from `DATABASE_URL` or `POSTGRES_DB`, `POSTGRES_USER`, and `POSTGRES_PASSWORD`; no credentials are embedded in Python source.
- Pinned `psycopg==3.3.5` and `psycopg-binary==3.3.5` in the resolved requirements files.
- Verified backend-to-PostgreSQL connectivity: `PostgreSQL connection ready; pgvector=0.8.6`.
- Verified `postgres_data` persistence across a PostgreSQL container restart with a temporary row; the temporary table was then removed.
- Full regression suite: `39 passed, 2 warnings`.
- PostgreSQL storage remains opt-in (`GALLERY_STORAGE=sqlite` by default); automatic schema creation and actual gallery migration are TASK 3 and TASK 4.

## TASK 3 — Alembic migrations

- Added Alembic configuration and revision `0001_gallery_pgvector`.
- The migration creates pgvector, `gallery_items` (including CSV position, BBox, SHA-256, 512-D vector and metadata) and `gallery_state` (fingerprints, CSV hash, size and build state).
- Docker now executes `alembic upgrade head` before FastAPI starts.
- Verified existing PostgreSQL database receives revision `0001_gallery_pgvector` and both tables.
- Verified a clean PostgreSQL volume: removed only `car-classification-msk_postgres_data`, started Compose again, and migration recreated the schema automatically; `gallery_items` starts empty.
- Regression after migration setup: `39 passed, 2 warnings`.
- TASK 4 remains: populate these tables and make PostgreSQL the operational gallery cache after parity tests.
