# Журнал работ — 21 сентября 2026

## Планирование

- В backlog добавлена отдельная DevOps-задача: Multi-stage Docker deployment.
- Источник: CODEX_DOCKER_MULTISTAGE_DEPLOYMENT_PLAN.md.
- Задача запланирована после TASK 4–6, так как её cold/warm-start проверки требуют рабочей PostgreSQL gallery и подтверждённого parity с SQLite.
- Содержит multi-stage Dockerfile, entrypoint, Docker healthcheck, offline runtime, cold/warm start, graceful shutdown, image-size и CPU/GPU profile проверки.

## TASK 4 — PostgreSQL gallery lifecycle

- Extended the gallery repository contract with GalleryBuildState: encoder and preprocessing fingerprints, CSV SHA-256 and per-image SHA-256.
- PostgreSQL now writes complete gallery metadata: CSV position, BBox, embedding, image SHA-256, encoder fingerprint and JSON metadata.
- gallery_state records the complete cache identity, source CSV SHA-256, gallery size and schema version.
- Gallery replacement is transactional: the old gallery remains intact if a rebuild fails before commit.
- Added explicit Gallery cache miss and cache hit startup logs.
- SQLite regression suite remains green: 39 passed, 2 warnings.
- Docker cold start with GALLERY_STORAGE=postgres built and persisted 750 gallery objects; gallery_state reports size 750.
- Warm restart reported Gallery cache hit; reusing persisted embeddings with no OSNet progress output.
- PostgreSQL API ranking for query 486dd80d22f94a5496b3e670b19399a7 returned the baseline Top-10; rank 1 is df7ea50171af48df81584ab7e8107d15 with raw cosine 0.7861639261245728.
- PostgreSQL storage is currently running for integration verification; formal SQLite/PostgreSQL parity fixtures are TASK 6.

## TASK 5 — exact pgvector cosine search

- Added exact PostgresGalleryRepository.search_cosine using pgvector cosine distance: ORDER BY embedding cosine-distance query-vector, then CSV position for deterministic ties.
- In PostgreSQL mode raw cosine scores, response similarity and confidence/refusal now come from pgvector; SQLite keeps NumPy exact cosine as its reference path.
- Full-gallery exact retrieval is intentional: existing k-reciprocal reranking can promote a candidate outside a raw Top-K, so limiting candidates would change the baseline Top-10.
- Existing reranker, model, preprocessing, threshold and HTTP contract were not changed.
- Regression suite: 39 passed, 2 warnings.
- Real-query check: maximum absolute pgvector/NumPy raw cosine difference was 1.1920928955078125e-07 and raw Top-50 order was identical.
- API ranking for query 486dd80d22f94a5496b3e670b19399a7 retained the baseline Top-10; pgvector raw similarity differs only by float32-level rounding.
- TASK 6 will formalize parity across stored fixtures, refusal and exports.

## TASK 6 — SQLite/PostgreSQL parity tests

- Added backend.parity: fixture-based automated comparison of persisted gallery embeddings, raw cosine, raw Top-50, reranked Top-10 and refusal between SQLite and PostgreSQL.
- Compared all three frozen baseline queries from tests/fixtures/sqlite_baseline.json.
- Gallery embeddings are bytewise equal; all raw Top-50, reranked Top-10 and refusal decisions are identical.
- Maximum fixture raw cosine difference is 2.384185791015625e-07, below the 2e-06 tolerance.
- Added evaluator --output to generate independent artifact sets without overwriting baseline artifacts.
- Generated and validated SQLite and PostgreSQL export sets: 1110 submission rows, 1051 candidate rows and embeddings shape 1860 by 512 float32.
- submission.csv, embeddings.npy and export_manifest.json are identical; candidates IDs are identical and maximum confidence difference is 2.086162567138672e-07.
- Baseline metrics are identical except measured benchmark time, which is intentionally excluded.
- Regression suite: 39 passed, 2 warnings.
- TASK 7 can make PostgreSQL the default storage; SQLite remains the reference implementation until final integration review.

## TASK 7 — PostgreSQL default storage

- Changed Docker Compose default GALLERY_STORAGE from sqlite to postgres.
- Updated .env.example: PostgreSQL is the default deployment mode; SQLite is explicit reference/debug mode.
- Kept the core fallback as SQLite so isolated unit tests and reference runs do not require a live database.
- Added gallery_storage to GET /api/health for runtime verification without exposing credentials.
- Verified docker compose config with no override resolves GALLERY_STORAGE=postgres.
- Verified backend startup reports Gallery cache hit and health reports PostgresGalleryRepository with a 750-item gallery.
- Updated README and PROJECT_PLAN to reflect PostgreSQL + pgvector as deployment storage and SQLite as reference implementation.
- Regression suite: 39 passed, 2 warnings.
- TASK 8 / Multi-stage deployment remains next DevOps milestone.

## TASK 8 — Multi-stage Docker deployment

- Dockerfile converted to builder/runtime stages: builder creates a venv from pinned runtime requirements; runtime receives only the venv, API code, frontend, model weights and Alembic files.
- Added docker/entrypoint.sh: validates DATASET_DIR and required organizer files, runs Alembic migrations, then uses exec to hand over signals to the API process.
- Runtime continues to run as uid 10001; dataset remains read-only through Compose mounts.
- Added backend healthcheck via local GET /api/health after PostgreSQL Compose health dependency.
- Extended .dockerignore so env files, development requirements, docs and journals are excluded from the build context.
- Verified startup and graceful shutdown through Uvicorn lifecycle logs.
- Rebuilt the image successfully; final runtime image size is 498,153,998 bytes (about 498 MB), compared with 490,822,353 bytes before this task. The small increase comes from the copied venv layout; builder layers and pip cache are absent from the runtime stage.
- Final Compose backend is healthy and GET /api/health reports PostgresGalleryRepository with 750 persisted gallery rows.
- Graceful stop was verified: Uvicorn completes its shutdown lifecycle after Docker stop; the normal Compose restart reports Gallery cache hit with no OSNet inference.
- Isolated cold-start verification used a separate Compose project and empty PostgreSQL volume on port 8001: it built all 750 gallery embeddings and became healthy. The temporary project and its volumes were then removed; the main service and database were untouched.
- Offline runtime verification passed with `--network none` and successful imports of FastAPI, ONNX Runtime and psycopg; no installation step exists in the runtime entrypoint.

## TASK 9 — Комплексное тестирование (в работе)

- Added an isolated Docker test stage and docker-compose.test.yml so pytest dev dependencies do not enter the production runtime image.
- Added PostgreSQL integration coverage for migration-backed pgvector repository: ping, gallery round trip, exact cosine ranking and cache-state invalidation.
- Initial isolated test compose run passed: 40 tests, 2 upstream deprecation warnings, 2.22 seconds.
- Smoke checks against the running PostgreSQL deployment passed: artifact validation confirms 1110 queries, 750 gallery objects, 1860×512 float32 embeddings and 1051 accepted candidates; ranking returns Top-10 in 77.057 ms and candidates mode with threshold 1 returns a refusal in 45.449 ms.
- Added API coverage for valid but unsupported GIF uploads (415) and oversized uploads above 15 MiB (413).
- Final isolated test compose run passed: 41 tests, 2 upstream deprecation warnings, 2.48 seconds. The temporary test containers and network were removed after the run.

## TASK 10 — Backend confidence API and Docker recheck

- Frontend audit found that upload, BBox, Top-N, rejection and JSON export flows are already implemented; further frontend changes were deferred by the user.
- Added explicit API confidence: maximum raw cosine over the complete gallery, computed in the same search pass so PostgreSQL does not receive a duplicate query.
- Rebuilt the production Docker image and verified healthy startup, migrations, PostgresGalleryRepository, pgvector 0.8.6, 750 persisted gallery objects and valid export artifacts.
- Real query 486dd80d22f94a5496b3e670b19399a7 returned confidence 0.786163866519928, equal to its maximum raw cosine.
- Final isolated backend/Docker regression run passed: 41 tests, 2 upstream deprecation warnings, 2.89 seconds. Temporary test resources were removed. Frontend files were not changed.

## TASK 11 — One-command offline inference

- Competition requirement review: mandatory deliverable is one isolated command that consumes images/CSV and produces submission.csv, embeddings.npy and candidates.csv without Internet. PostgreSQL and web UI are optional demo features.
- Added Compose profile `inference`: it runs backend.evaluate --export in the existing runtime image with SQLite and no PostgreSQL dependency.
- Compose configuration is valid. The documented inference command completed with exit code 0 and wrote submission.csv, embeddings.npy, candidates.csv, export_manifest.json, metrics, splits and SQLite cache directly into ignored ./artifacts.
- Strict artifact validation passed: 1110 queries, 750 gallery objects, embeddings shape 1860×512 float32, 1110 submission rows, 1051 candidate rows and 59 refusals.
- Full inference was also executed from the prepared image with Docker network none; it completed without PostgreSQL or external network access. A second network-none validator run confirmed the host artifacts.

## План передачи образа жюри

- Added docs/CONTEST_IMAGE_DELIVERY.md: Linux amd64 build, architecture check, one-command inference verification, no-network validator, docker save archive and SHA-256 handoff steps.
- The archive is an offline delivery supplement; source repository and Docker configuration remain mandatory.
- Current runtime is CPU-only. GPU image must be implemented and benchmarked separately before it can be claimed.

## TASK 12 — PostgreSQL-only runtime

- Switched every Compose runtime path, including the one-command `inference` profile, to PostgreSQL + pgvector. `inference` now waits for the database healthcheck and runs migrations through the normal entrypoint.
- Runtime selection now accepts only `GALLERY_STORAGE=postgres`; neither API nor export can select SQLite.
- SQLite remains only as an explicitly injected unit-test fixture and historical parity tool; it is not exposed in Docker, `.env.example`, README commands or application configuration.
- Updated README, project plan and image handoff instructions: delivery now includes the application image plus the pinned pgvector image, and runtime offline means no external downloads while Docker services communicate internally.
- Verification: isolated Docker suite passed 41 tests (2 upstream deprecation warnings). PostgreSQL-backed `inference` export passed artifact validation: 1110 queries, 750 gallery objects, embeddings 1860×512 float32, 1110 submission rows, 1051 candidates and 59 refusals.
- Recreated the main API container; `/api/health` reports `PostgresGalleryRepository` and gallery size 750.

## TASK 13 — Конкурсный аудит, документация и commit preparation

- Added short module summaries to the FastAPI, inference, core search and PostgreSQL repository code, plus the Docker entrypoint comment.
- Added docs/RUNBOOK.md, docs/TEST_SUMMARY.md and docs/CONTEST_COMPLIANCE_AUDIT.md.
- Audit result: export, artifact formats, PostgreSQL + pgvector, Docker Compose and API are verified; offline Swagger UI, GPU A5000 benchmark/profile and presentation remain open.

## TASK 14 — Offline Swagger UI

- Vendored Swagger UI 5.31.0 CSS/JS, FastAPI favicon and Apache-2.0 LICENSE under frontend/vendor/swagger-ui/.
- Replaced FastAPI default CDN `/docs` with a local route that serves assets from `/static/vendor/swagger-ui/`; ReDoc is disabled because it was not required and would otherwise use external assets.
- Added an API regression assertion that `/docs` contains local assets and no HTTPS URL.

## TASK 15 — План web-ветки и финальной поставки

- README startup section was rewritten around Docker Compose; obsolete venv-based normal launch instructions were removed.
- Added next steps: web branch with React, Tailwind and locally bundled shadcn/ui; Linux amd64 image delivery archive; optional GPU benchmark; presentation and demo.
