# Журнал работ — 18 сентября 2026

## Выполнено

- Проанализирована текущая реализация Vehicle ReID и требования задания.
- Добавлен контейнерный запуск CPU-версии: `Dockerfile`, `docker-compose.yml` и `.dockerignore`.
- Точка запуска backend настроена на `0.0.0.0`, что позволяет открыть сервис через проброшенный Docker-порт.
- Добавлен краткий README с запуском, экспортом submission-артефактов и структурой подключаемого датасета.
- Docker-образ собран; ONNX-модель успешно импортирована внутри контейнера.

## Текущее состояние архитектуры

- Backend: FastAPI + ONNX Runtime.
- Хранилище gallery: SQLite (`artifacts/gallery.sqlite3`).
- Поиск: точный cosine search в памяти по NumPy-матрице, затем k-reciprocal reranking.
- PostgreSQL и pgvector пока не подключены.

## Следующие решения

- Найти или предоставить путь к Git-репозиторию и ветке `web` перед переключением ветки.
- Решить, заменяем ли SQLite на PostgreSQL + pgvector до доработки пользовательского интерфейса.
